---
phase: v2-02-iconic-landmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - metropolis-3d-city-7.html
autonomous: true

must_haves:
  truths:
    - "Luxor Pyramid is visible from distance as a black triangular shape"
    - "Paris Eiffel Tower is recognizable by its lattice silhouette"
    - "Both landmarks are positioned along The Strip"
    - "Scale feels proportional to The Strip width"
  artifacts:
    - path: "metropolis-3d-city-7.html"
      provides: "mkLuxorPyramid() and mkParisEiffelTower() functions"
      contains: "function mkLuxorPyramid"
    - path: "metropolis-3d-city-7.html"
      provides: "Vegas landmarks array for tracking"
      contains: "vegasLandmarks"
  key_links:
    - from: "mkLuxorPyramid()"
      to: "scene.add"
      via: "init() sequence"
      pattern: "scene\\.add.*luxor"
    - from: "mkParisEiffelTower()"
      to: "scene.add"
      via: "init() sequence"
      pattern: "scene\\.add.*eiffel"
---

<objective>
Create Luxor Pyramid and Paris Eiffel Tower landmarks for Las Vegas Strip

Purpose: Establish the first two iconic Vegas landmarks with distinctive silhouettes recognizable from any viewing angle. Luxor's black pyramid and Paris's Eiffel Tower are among the most identifiable Vegas structures.

Output: Two mk*() functions creating 3D landmark models, positioned on The Strip parking lots, tracked in vegasLandmarks array for future lighting phases.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/REQUIREMENTS-v2.md
@.planning/phases/v2-01-vegas-infrastructure/v2-01-02-SUMMARY.md
@metropolis-3d-city-7.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Luxor Pyramid</name>
  <files>metropolis-3d-city-7.html</files>
  <action>
Create mkLuxorPyramid() function returning a THREE.Group containing:

1. **Pyramid structure** using ConeGeometry (4 sides = pyramid):
   - Base width: ~25 units (fits parking lot, visible scale)
   - Height: ~30 units (distinctive triangular profile)
   - Material: Dark glass (0x1a1a1a or 0x111111, metalness: 0.9, roughness: 0.1 for reflective glass)
   - 4 radial segments to create pyramid (not cone)

2. **Base platform**:
   - Slightly larger than pyramid base (~27 units)
   - Low height (0.5-1 unit)
   - Tan/sandstone color (0xc4a777) matching desert theme

3. **Sphinx hint** (optional simple shape):
   - Small box (3x2x1.5) positioned in front of pyramid
   - Same sandstone color as platform
   - Suggests sphinx without complex modeling

4. **Position**: West side of Strip (negative X offset from Strip center)
   - Use theStrip.userData.centerX for Strip position
   - Place in parking lot area: centerX - stripWidth/2 - sidewalkWidth - 15
   - Z position: near south end of Strip (startZ + 15)

5. **Tracking**:
   - Add to vegasLandmarks[] array (create if not exists)
   - Store userData: { type: 'luxor', hasBeam: true } for Phase 3 lighting

Call mkLuxorPyramid() in init() after mkTemperatureBillboard(), add result to scene.

Avoid: Don't try to model the actual Luxor hotel wings - just the iconic pyramid shape. Don't add the sky beam yet (that's Phase 3 LITE-03).
  </action>
  <verify>
Reload metropolis-3d-city-7.html. Fly to Vegas zone. Black pyramid visible on west side of Strip near south end. Pyramid has 4 triangular faces (not round cone). Base platform visible. No console errors.
  </verify>
  <done>
Luxor Pyramid landmark exists as identifiable black triangular structure on west side of Strip, with sandstone base platform.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Paris Eiffel Tower</name>
  <files>metropolis-3d-city-7.html</files>
  <action>
Create mkParisEiffelTower() function returning a THREE.Group containing:

1. **Tower structure** (simplified lattice silhouette):
   - Total height: ~35-40 units (taller than pyramid for variety)
   - Use THREE.CylinderGeometry with small top/large bottom radii for tapered shape
   - Color: Dark iron/bronze (0x4a4a4a or 0x5c4033)
   - Build in 3 sections to suggest the real tower's tiers:
     - Base section: Wide legs (4 cylinders angled inward, 0.5 radius, ~12 units tall)
     - Middle section: Narrower body (cylinder, 2 radius bottom to 1 radius top, ~12 units)
     - Top section: Spire (thin cylinder, 0.5 radius to 0.2, ~10 units)

2. **Observation deck platforms** (2):
   - First platform at ~12 units height (end of legs)
   - Second platform at ~24 units height (end of middle section)
   - Use flat cylinders (radius 3-4, height 0.3)
   - Slightly lighter color for contrast

3. **Base/foundation**:
   - Square platform using BoxGeometry (15x1x15)
   - Concrete gray (0x888888)

4. **Position**: East side of Strip (positive X offset from Strip center)
   - centerX + stripWidth/2 + sidewalkWidth + 12
   - Z position: middle of Strip (startZ + stripLength/2)

5. **Tracking**:
   - Add to vegasLandmarks[] array
   - Store userData: { type: 'paris', hasLights: true }

Call mkParisEiffelTower() in init() after mkLuxorPyramid(), add result to scene.

Note: The goal is silhouette recognition, not architectural accuracy. Four angled legs converging upward with observation platforms creates the iconic Eiffel shape.
  </action>
  <verify>
Reload page. Fly along Strip. Eiffel Tower visible on east side, middle of Strip. Tower has recognizable tapered shape with platforms. Clearly distinguishable from pyramid. No console errors.
  </verify>
  <done>
Paris Eiffel Tower landmark exists as identifiable tower structure on east side of Strip, with tapered profile and observation platforms suggesting the iconic shape.
  </done>
</task>

<task type="auto">
  <name>Task 3: Initialize vegasLandmarks tracking array</name>
  <files>metropolis-3d-city-7.html</files>
  <action>
1. Add global vegasLandmarks array near other Vegas globals (around line 193):
   ```javascript
   let vegasLandmarks = [];
   ```

2. Verify both landmarks are pushed to this array in their mk*() functions:
   ```javascript
   vegasLandmarks.push(pyramid); // in mkLuxorPyramid
   vegasLandmarks.push(tower);   // in mkParisEiffelTower
   ```

3. Add comment above array explaining purpose:
   ```javascript
   // Vegas landmarks for lighting system (Phase 3)
   let vegasLandmarks = [];
   ```

This array enables Phase 3 to iterate over landmarks for night lighting effects without searching scene graph.
  </action>
  <verify>
Open browser console. After page load, type `vegasLandmarks.length` - should return 2. Type `vegasLandmarks[0].userData.type` - should return 'luxor' or 'paris'.
  </verify>
  <done>
vegasLandmarks array exists globally, contains both landmarks, each with appropriate userData for lighting system integration.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Load metropolis-3d-city-7.html - no console errors
2. Fly from Metropolis along Route 66 highway to Vegas
3. Black Luxor pyramid visible on west side of Strip (south end)
4. Eiffel Tower visible on east side of Strip (middle)
5. Both landmarks identifiable from silhouette at distance
6. Fly close to each - architectural details visible
7. Console: `vegasLandmarks.length === 2`
8. Console: Both landmarks have userData.type set
</verification>

<success_criteria>
- Luxor Pyramid: Black triangular shape, 4 faces, sandstone base
- Paris Eiffel Tower: Tapered tower with platforms, recognizable silhouette
- Both positioned appropriately on Strip parking lots
- vegasLandmarks array tracks both for Phase 3
- No console errors
- Scale feels proportional to Strip width (landmarks ~2x Strip width)
</success_criteria>

<output>
After completion, create `.planning/phases/v2-02-iconic-landmarks/v2-02-01-SUMMARY.md`
</output>
