---
phase: v2-02-iconic-landmarks
plan: 02
type: execute
wave: 2
depends_on: ["v2-02-01"]
files_modified:
  - metropolis-3d-city-7.html
autonomous: true

must_haves:
  truths:
    - "Bellagio has curved facade visible from Strip"
    - "Bellagio fountain lake is visible as water surface in front of building"
    - "Caesars Palace has Roman columns at entrance"
    - "Both landmarks are positioned along The Strip"
  artifacts:
    - path: "metropolis-3d-city-7.html"
      provides: "mkBellagio() and mkCaesarsPalace() functions"
      contains: "function mkBellagio"
    - path: "metropolis-3d-city-7.html"
      provides: "Fountain lake geometry with water material"
      contains: "fountainLake"
  key_links:
    - from: "mkBellagio()"
      to: "vegasLandmarks"
      via: "array push"
      pattern: "vegasLandmarks\\.push.*bellagio"
    - from: "mkCaesarsPalace()"
      to: "vegasLandmarks"
      via: "array push"
      pattern: "vegasLandmarks\\.push.*caesars"
---

<objective>
Create Bellagio with fountain lake and Caesars Palace landmarks for Las Vegas Strip

Purpose: Add two more iconic Vegas landmarks. Bellagio is famous for its elegant curves and fountain lake (setting up Phase 4's fountain show). Caesars Palace represents classic Vegas glamour with its Roman architecture.

Output: Two mk*() functions creating 3D landmark models with distinctive architectural features, positioned on Strip parking lots, fountainLake reference stored for Phase 4 animation.
</objective>

<execution_context>
@/Users/peter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP-v2.md
@.planning/STATE.md
@.planning/REQUIREMENTS-v2.md
@.planning/phases/v2-02-iconic-landmarks/v2-02-01-SUMMARY.md
@metropolis-3d-city-7.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bellagio with Fountain Lake</name>
  <files>metropolis-3d-city-7.html</files>
  <action>
Create mkBellagio() function returning a THREE.Group containing:

1. **Main building** (curved facade):
   - Use BoxGeometry for simplicity, but rotate slightly to suggest curve
   - OR create 3 angled sections forming shallow arc facing Strip
   - Width: ~30 units, Depth: ~15 units, Height: ~25 units
   - Color: Cream/beige (0xf5f0e6) for elegant Italianate look
   - Add darker accent band at roof level (0xc4a777)

2. **Tower section** (behind main):
   - Taller box (8x35x10) centered behind main building
   - Same cream color
   - Suggests the hotel tower

3. **Fountain lake** (CRITICAL for Phase 4):
   - PlaneGeometry or EllipseGeometry (CylinderGeometry with very low height)
   - Size: ~35 wide x 20 deep (substantial water feature)
   - Position: In front of building facing Strip
   - Material: MeshStandardMaterial with:
     - Color: 0x4fc3f7 (light blue)
     - Opacity: 0.8, transparent: true
     - Metalness: 0.3, roughness: 0.1 (reflective water)
   - Store reference: `bellagio.userData.fountainLake = lake`

4. **Lake border**:
   - Curved/rectangular rim around lake
   - White/cream color (0xeeeeee)
   - BoxGeometry segments around perimeter (0.3 height, 0.5 width)

5. **Position**: West side of Strip, north of Luxor
   - centerX - stripWidth/2 - sidewalkWidth - 20
   - Z position: middle-north area (startZ + stripLength * 0.6)
   - Fountain lake between building and Strip

6. **Tracking**:
   - Add to vegasLandmarks[]
   - Store userData: { type: 'bellagio', hasFountain: true, fountainLake: lake }

Global reference for Phase 4:
```javascript
let bellagioFountainLake = null; // Set in mkBellagio()
```

Call mkBellagio() in init() after existing landmarks.
  </action>
  <verify>
Reload page. Fly to north-middle of Strip west side. Bellagio building visible with cream facade. Blue fountain lake visible in front. Lake has visible border. No console errors. Console: `bellagioFountainLake !== null`
  </verify>
  <done>
Bellagio landmark exists with elegant facade and fountain lake (water surface) in front, lake reference stored for Phase 4 fountain show.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Caesars Palace</name>
  <files>metropolis-3d-city-7.html</files>
  <action>
Create mkCaesarsPalace() function returning a THREE.Group containing:

1. **Main building**:
   - Large rectangular structure: 40 wide x 20 deep x 22 high
   - Color: White/cream marble (0xf8f8f0)
   - Roughness: 0.4, metalness: 0.1 (polished stone look)

2. **Roman columns** (CRITICAL identifying feature):
   - 6-8 columns across front entrance
   - CylinderGeometry: radius 0.5, height 10
   - Color: White marble (0xf0f0f0)
   - Spaced evenly across entrance portico
   - Position: Front face of building

3. **Entrance portico**:
   - Wide overhang/roof above columns
   - BoxGeometry: 25 wide x 8 deep x 2 high
   - Supported by the columns
   - Slightly darker than building (0xe8e8e0)

4. **Column capitals** (top detail):
   - Small wider cylinders or boxes on top of each column
   - Radius 0.7, height 0.5
   - Suggests classical column tops

5. **Central dome** (optional):
   - SphereGeometry (half sphere) on roof center
   - Radius: 4, positioned at building top center
   - Gold tint (0xd4af37, metalness: 0.7)

6. **Grand staircase hint**:
   - 2-3 step boxes leading up to portico
   - Each step: 20 wide x 3 deep x 0.5 high
   - White color matching building

7. **Position**: East side of Strip, south of Eiffel Tower
   - centerX + stripWidth/2 + sidewalkWidth + 25
   - Z position: south-middle area (startZ + stripLength * 0.3)
   - Entrance facing Strip (columns visible from road)

8. **Tracking**:
   - Add to vegasLandmarks[]
   - Store userData: { type: 'caesars', hasColumns: true }

Call mkCaesarsPalace() in init() after mkBellagio().
  </action>
  <verify>
Reload page. Fly to south-middle of Strip east side. Caesars Palace visible with white facade. Roman columns clearly visible at entrance. Portico roof visible above columns. Grand entrance feel. No console errors.
  </verify>
  <done>
Caesars Palace landmark exists with Roman columns at grand entrance, white marble appearance, classical architecture feel.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify landmark positions and scale</name>
  <files>metropolis-3d-city-7.html</files>
  <action>
Review and adjust all 4 landmark positions to ensure:

1. **No overlaps**: Each landmark has clear space around it
   - Minimum 10 units between landmarks
   - Check Z positions don't collide

2. **Strip visibility**: All landmarks visible from Strip road
   - Buildings set back enough that fountains/entrances face Strip
   - Not so far back they're hard to see

3. **Scale consistency**:
   - Luxor: ~25 wide x 30 tall
   - Eiffel: ~15 wide x 40 tall
   - Bellagio: ~30 wide x 25 tall (+ lake)
   - Caesars: ~40 wide x 22 tall

4. **Position summary** (update as needed):
   - Luxor: West side, south (Z ~ startZ + 15)
   - Bellagio: West side, middle-north (Z ~ startZ + stripLength * 0.6)
   - Eiffel: East side, middle (Z ~ startZ + stripLength * 0.5)
   - Caesars: East side, south-middle (Z ~ startZ + stripLength * 0.3)

5. **Console verification**:
   - vegasLandmarks.length === 4
   - All have distinct userData.type values

If positions need adjustment, update the X and Z coordinates in the respective mk*() functions.
  </action>
  <verify>
Fly along entire Strip from south to north. All 4 landmarks visible and identifiable. No visual overlaps. Good spacing. Each landmark has unique silhouette. Console: `vegasLandmarks.map(l => l.userData.type)` shows ['luxor', 'paris', 'bellagio', 'caesars'] or similar.
  </verify>
  <done>
All 4 landmarks properly positioned along Strip with appropriate scale and spacing, no overlaps, all visible from Strip road.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Load page - no console errors
2. Fly along Strip south to north
3. West side: Luxor (south), Bellagio (north) - both visible
4. East side: Caesars (south), Eiffel (middle) - both visible
5. Bellagio fountain lake clearly visible as blue water
6. Caesars columns visible at entrance
7. No landmark overlaps
8. Console: `vegasLandmarks.length === 4`
9. Console: `bellagioFountainLake` exists
</verification>

<success_criteria>
- Bellagio: Curved/elegant facade, blue fountain lake in front, lake reference stored
- Caesars Palace: Roman columns at entrance, grand portico, white marble look
- Both positioned appropriately on Strip parking lots
- All 4 landmarks (including Plan 01) have good spacing
- vegasLandmarks array now has 4 entries
- bellagioFountainLake global reference set for Phase 4
- No console errors
</success_criteria>

<output>
After completion, create `.planning/phases/v2-02-iconic-landmarks/v2-02-02-SUMMARY.md`
</output>
